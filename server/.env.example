DATABASE_URL=postgresql://trash2cash:trash2cash@localhost:5432/trash2cash?schema=public
PORT=4000
JWT_ACCESS_SECRET=replace-with-strong-secret
JWT_REFRESH_SECRET=replace-with-strong-secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
CORS_ORIGIN=http://localhost:3000

# Gemini API (required for /api/chat)
GEMINI_API_KEY=replace-with-gemini-key
# Optional model override (default: gemini-1.5-flash)
# GEMINI_MODEL=gemini-1.5-flash

# Redis cache (set this in your local .env, do NOT commit real secrets)
# Example format:
# REDIS_URL=redis://default:<PASSWORD>@redis-10658.c212.ap-south-1-1.ec2.cloud.redislabs.com:10658
# CACHE_TTL=600
